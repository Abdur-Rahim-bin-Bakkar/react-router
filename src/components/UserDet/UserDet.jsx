import React from 'react';
import { useLoaderData } from 'react-router';

const UserDet = () => {
    return (
        <div>
            <h1>user Det</h1>
        </div>
    );
};

export default UserDet;